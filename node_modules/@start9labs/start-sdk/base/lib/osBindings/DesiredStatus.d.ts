import type { StartStop } from "./StartStop";
export type DesiredStatus = {
    main: "stopped";
} | {
    main: "restarting";
} | {
    main: "running";
} | {
    main: "backing-up";
    onComplete: StartStop;
};
