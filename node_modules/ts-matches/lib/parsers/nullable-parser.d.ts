import { IParser, OnParse } from "./interfaces";
import { Parser } from "./parser";
export declare class NullableParser<A, B> implements IParser<A | null, B | null> {
    readonly parent: Parser<A, B>;
    readonly description: {
        readonly name: "Nullable";
        readonly children: readonly [Parser<A, B>];
        readonly extras: readonly [];
    };
    constructor(parent: Parser<A, B>, description?: {
        readonly name: "Nullable";
        readonly children: readonly [Parser<A, B>];
        readonly extras: readonly [];
    });
    parse<C, D>(a: A, onParse: OnParse<A | null, B | null, C, D>): C | D;
}
