import { IParser, NonNull, OnParse } from "./interfaces";
import { Parser } from "./parser";
export declare class NullishParsed<A, B, B2> implements IParser<A | null, NonNull<B, B2>> {
    readonly parent: Parser<A, B>;
    readonly defaultValue: B2;
    readonly description: {
        readonly name: "Nullable";
        readonly children: readonly [Parser<A, B>];
        readonly extras: readonly [B2];
    };
    constructor(parent: Parser<A, B>, defaultValue: B2, description?: {
        readonly name: "Nullable";
        readonly children: readonly [Parser<A, B>];
        readonly extras: readonly [B2];
    });
    parse<C, D>(a: A, onParse: OnParse<A | null, NonNull<B, B2>, C, D>): C | D;
}
